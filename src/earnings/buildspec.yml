version: 0.2

phases:
  build:
    commands:
      - cd $APP_DIR
      - pip install -r requirements.txt -t /artifacts/python
      - cp *.py /artifacts
artifacts:
  files:
    - "**/*"
  base-directory: /artifacts
  discard-paths: no
